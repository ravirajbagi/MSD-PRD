# PaperToNotebook — environment variables
# Copy this file to .env.local and fill in the values.

# Optional: GitHub personal access token for creating Gists (anonymous works too,
# but tokens get higher rate limits).
# Generate at: https://github.com/settings/tokens — needs "gist" scope.
GITHUB_TOKEN=
